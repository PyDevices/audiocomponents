# Accuracy Dossier — `tr808` (Roland TR-808 Rhythm Composer)

**Module:** `audiocomponents/lib/audioinstruments/tr808.py` (277 lines)
**Proposed grade:** **gold**, with an acquisition gap stated below.
**Reference settings (D3):** no known-knob single-hit capture exists
anywhere reached; this dossier uses the **statistical envelope** across a
pack (roadmap D3).

## 1. Reference and grade

**Primary — capture:** Sample Magic TR-808 pack via Splice, 107 hits
"captured from the legendary Roland TR-808," page states "100% royalty
free and cleared for commercial use"
(https://splice.com/sounds/packs/sample-magic/sample-magic-tr-808/samples).
Clears gold's bar: capture reached, provenance stated, license read.
**Acquisition gap:** anonymous fetch only reaches "-scrambled" preview
MP3s whose S3-signed URLs expire before download (`SignatureDoesNotMatch`)
— the real WAVs need a free Splice account, a manual step for Brad.

**Fallback used for the measured numbers below:** MusicRadar SampleRadar
808 pack
(https://www.musicradar.com/news/sampleradar-378-free-808-drum-samples),
license on page: royalty-free, no redistribution — provenance unstated,
so this is a **statistical cross-check, not the oracle**. 219 hits
measured in hand at
`.reference-captures/tr808/musicradar_hits_stats.json`.

**Literature:** DAFx-14, Werner/Abel/Smith, "A Physically-Informed,
Circuit-Bendable, Digital Model of the Roland TR-808 Bass Drum Circuit"
(https://www.dafx.de/paper-archive/2014/dafx14_kurt_james_werner_a_physically_informed,_ci.pdf);
TR-808 schematics
(https://www.synthfool.com/docs/Roland/TR_Series/TR808/TR-808_Schematics.pdf).

## 2. License call, per source

| Source | License as read | Where read |
|---|---|---|
| Splice / Sample Magic pack | "100% royalty free and cleared for commercial use" | samples page above |
| MusicRadar SampleRadar pack | royalty-free, no redistribution; provenance unstated | musicradar.com page above |
| DAFx-14 Werner/Abel/Smith paper | license unverified, no CC line found; treated copyleft-equivalent: re-derive the math, never copy | dafx.de archive above |
| Synthfool schematics PDF | license unverified, scanned archive, no rights statement; topology reading only, never redistributed | synthfool.com PDF above |

Nothing above is permissive source code; nothing is ported. Captures are
measured as oracles; the paper's math is re-derived below, not transcribed.

## 3. Hardware structure vs the module

12 analog drum voices per Wikipedia's infobox (polyphony 12 / timbrality
12): BD, SD, low/mid/hi tom, rimshot, hand clap, cowbell, cymbal,
open/closed hi-hat, congas, claves, maracas — several sharing physical
circuits (https://en.wikipedia.org/wiki/Roland_TR-808). `NOTE_MAP`
(tr808.py:42-56) has 12 entries but **omits congas and maracas**: their
MIDI notes fall to the generic noise+bandpass "Fallback" branch
(tr808.py:244-248), not a dedicated voice. Sibling modules set a
note-number convention worth reusing: `linndrum.py:55-57` maps Conga
Hi/Mid/Lo to 62/63/64; `cr78.py:55` maps Maracas to 70.

Proposed structural change: give Congas (62/63/64) and Maracas (70)
dedicated branches, driven by the measured targets below (Congas n=30,
Maracas n=11), in place of the fallback — additive within the existing
12-voice count, not an expansion of it.

**Superseded 2026-09-03 — the code this described is gone.** It read:
`MAX_VOICES = 16` (tr808.py:115) is a Note pool, not a hardware-voice count,
and `_support.trigger_voice`/`steal_oldest` mix units by comparing held
hit-*groups* against the incoming hit's raw Note count. `MAX_VOICES` no longer
exists in `tr808.py`, and `trigger_voice` was removed as dead code carrying a
non-terminating loop (#10); the file now names it only in a comment saying
why. Recorded rather than deleted because the *observation* was right and
outlived its code: predicting capacity is what was wrong, and `press_voice`
replaced it by asking the engine instead.

What survives, and still matters: several voices are multi-oscillator
internally — **SD=3, Clap=3, Cymbal=2, BD=2, Rimshot=2, Cowbell=2**. That
count is a fact about the machine, and it is why the rimshot needed a second
oscillator to reach the reference tail (see §5c): the hardware rimshot is a
bridged-T ringing alongside a pulse, and one oscillator cannot be both.

The kit now holds **exactly 14 permanent Notes**, at the engine's cap of 14.
That is deliberate and verified, not incidental: a full-kit hit retriggered
twice at the cap holds total energy within 3% and drops no voice. It became
safe only when audioif#8 (an at-cap re-press evicting a bystander) was fixed
by `b420dac`; before that the kit deliberately sat at 13.

## 4. Modeling approach, by voice group

**Bass drum** (tr808.py:150-160): the DAFx-14 mechanism, re-derived from
its text, not copied — trigger → pulse shaper → bridged-T bandpass
network with a feedback buffer; a retrigger pulse re-injected a few ms
after attack raises the center frequency **more than an octave** briefly
(the attack "click/jump"), then feedback leakage produces a slow downward
pitch "sigh." VR6 varies feedback amount and, with it, decay time.
Roland's tuning chart nominal is 56 Hz, consistent with the measured
53.8 Hz median below.

The current model only bends *down* from `bd_tune` via a one-shot `FALL`
LFO — it never jumps up first. Proposed: a two-stage bend (fast upward
jump >1 octave over ~2-5 ms, then exponential settle toward `bd_tune`),
with `bd_decay` driving settle tau — folding "VR6 feedback → decay" onto
the existing macro, no new one needed. The existing noise-click Note
(tr808.py:157-159) already mirrors the paper's separate pulse-shaper
stage; keep it, tighten HP cutoff/decay against the numbers below.

**Snare / toms / hats / cymbal / clap / cowbell / rimshot**
(tr808.py:161-227): existing multi-oscillator structures (tuned body +
noise-through-bandpass) already match the published block-diagram shape
for TR-808 percussion; no DAFx paper was found for these voices
specifically. MIT-licensed io-808 corroborates the general architecture
(https://github.com/vincentriemer/io-808) — read for shape, not ported.
Toolkit upgrade: swap the fixed-Q `synthio.Biquad` bandpass stages for
the corrected wide biquad with honest band edges (hat/cymbal bands sit
close to where the old biquad's error matters most); consider
`audiodynamics` transient shaping on the clap's triple-attack stack in
place of manually staggered `attack_time` offsets. No convolution is
proposed — no `audioconvolve.FRAMES` latency incurred.

## 5a. Cymbal band-share criterion, and the f_early row it replaces (2026-09-02)

The METAL rebuild fixed the hats and broke the crash. Voicing the bank once at
the hi-hat's band left the cymbal with **1.4%** of its power at 2-4 kHz where
this machine's own MusicRadar reference pack carries **63.9%** — level right,
body gone, all sizzle and no clang. Every stated §5 criterion passed while that
was true, which is the same criteria gap that let the 15 dB hat error through.

**Correction (2026-09-03).** This section previously recorded the reference
share as 31.3% and called the cymbal's 31.3% an exact match. 31.3% was *our*
measured value in both columns. Re-measured directly off the pack, the
reference median is **63.9%** (n=16, IQR 60.8-69.8; 13 of the 16 hits are
above 50%, the lowest 21.1%). The voicing was therefore only half-way, not
exact, and the "exact" reading came from comparing the model against itself.
The remaining gap is what #11 reported independently and what the corner move
below closes. Both columns of every row are now measured from their own
source.

The cymbal has its own voicing of the bank (`_METAL_CYM_*`), and its runtime
band-pass corner moved from 7000 Hz to 3500 Hz — the pack's own centre of
mass rather than a point above it. At 7000 Hz the voice put 15% of its power
above 8 kHz, where the machine carries 2.4%. Measured in the module, power
share per band:

| | before rebuild | own voicing | corner at 3500 | reference (n=16) |
|---|---|---|---|---|
| 1-2 kHz share | — | 1.8% | **5.2%** | 5.7% |
| 2-4 kHz share | 1.4% | 31.3% | **65.9%** | 63.9% |
| 4-8 kHz share | — | 51.9% | **26.6%** | 25.6% |
| 8-24 kHz share | — | 15.0% | **2.3%** | 2.4% |
| centroid | 9112 Hz | 7861 Hz | **5801 Hz** | 5238 Hz |
| tau | 0.672 s | 0.673 s | **0.673 s** | 0.775 s ±30% |

All four bands now land within 9% of the pack, the worst being 1-2 kHz at
-9%. The centroid improves from +50% to +10.7% but still reads high: the bank
is six square oscillators, and closing the last of it needs the cymbal's own
partial set rather than a filter. tau is unchanged and inside its band.

**The f_early row should be retired for this voice.** It reads 5531 Hz against
its 5814-9690 band, i.e. formally out — but that number is a marginal argmax
whose winner flips between 6460 and 5531 on differences as small as a test rig
versus the module, and §7 already recorded it as satisfied "by a razor-thin
peak in a now-dense spectrum, not by a robust feature". Choosing a voicing on
it is choosing on noise. **The 2-4 kHz band share is what actually caught this
fault** — 22x off before, within 3% after the corner move — and is proposed as
its replacement, at the corrected reference figure of **63.9% ±15%** (n=16,
IQR 60.8-69.8). It is a proposal, not an adopted criterion: replacing a §5 row
is Brad's call, and the row below still reads f_early.

That the band share was itself misread for a day is the argument for it, not
against it. The failure was not the measure but the comparison — our own
number sat in the reference column, so the check compared the model against
itself and could only ever pass. Any criterion can be defeated that way. When
this row is adopted, the reference figure needs re-deriving from the pack, not
copying from whatever the model currently reads.

Recorded rather than quietly traded: this change takes the cymbal from passing
a criterion that cannot discriminate to failing it, while matching the
reference on one that can.

## 5c. Rimshot, claves and tom tuning corrected (2026-09-03)

Three faults Brad heard and blessed the fix for on 2026-09-03. All measured
against this machine's own MusicRadar pack with the house ruler.

| | before | after | reference |
|---|---|---|---|
| Rimshot tau | 17.7 ms | **27.1 ms** | 27.1 ms (n=11) |
| Rimshot f_early / centroid | 445 / 3065 Hz | **445 / 3053 Hz** | 452 / 3092 Hz |
| Claves tau | 33.1 ms | **20.9 ms** | 20.8 ms (n=11) |
| Low Tom f_early | 93.8 Hz | **82.0 Hz** | 86.1 Hz (n=12) |
| Mid Tom f_early | 140.6 Hz | **128.9 Hz** | 129.2 Hz (n=12) |
| Hi Tom f_early | 187.5 Hz | **175.8 Hz** | 172.3 Hz (n=12) |

**The rimshot needed a second oscillator**, which is what §3 says the hardware
has. One oscillator cannot hold both a 452 Hz body and a 3092 Hz centre of
mass: pushing its passband up to reach the brightness makes the 3rd harmonic
dominate and `f_early` jumps to 1348 Hz. So the body keeps its own band and a
partner at 1900 Hz carries the ring. Its *amplitude* is the binding
constraint, not its level — above about 0.30 the partner wins the early
spectrum outright and `f_early` reads 2203 Hz instead of 445.

This took the kit from 13 permanent Notes to 14, at the cap. See §3.

**The toms were retuned in patch 0, not in the module defaults.** `lt_tune`
and friends are overwritten at construction by `PATCHES[0]`, so editing them
changes nothing — the first attempt did exactly that and measured no change.
Macros 8/9/10 went 74/71/71 → 59/48/47 through `logmap`, and the module
defaults were moved to match what patch 0 now produces so the two do not
disagree.

**Why §5 did not catch any of this:** it had no rimshot or claves row at all.
Both voices could sit 35% and 59% off their reference tails while every stated
criterion passed — the same criteria gap that let the 15 dB hat error and the
cymbal's missing body through. Rows for both are now in §5 above.

**A ruler limit worth recording.** Low Tom reads 82.0 Hz against a target of
86.1 and cannot do better. `measure_hits.dominant_freq` pads to 4096 bins at
48 kHz, so `f_early` is quantised to ~11.7 Hz steps down there: 82.0 and 93.8
are the only available readings and 86.1 falls between them. 82.0 is the
closer. Do not chase that residual by retuning — the synthesis frequency is
82.8 Hz, −3.8% from the pack, and it is the measurement that is coarse, not
the voice that is flat.

## 5. Acceptance criteria (statistical envelope, MusicRadar cross-check)

| Voice | Target | Tolerance |
|---|---|---|
| BD attack | jump >1 octave above settled pitch within a few ms, then sighs down | qualitative pass/fail on pitch trace |
| BD settled fundamental | 50–56 Hz | ±10 Hz |
| BD decay tau, short setting | 0.02–0.11 s (med 0.025 s, n=12) | span this end of the decay macro |
| BD decay tau, long setting | 0.12–0.90 s (med 0.203 s, n=17) | span this end of the decay macro |
| SD tau / centroid | 0.042 s / 4494 Hz | ±30% tau, ±20% centroid |
| CH tau / centroid | 0.024 s / 9687 Hz | ±30% / ±20% |
| OH tau | 0.173 s | ±30% |
| Cymbal tau / f_early | 0.775 s / 7752 Hz | ±30% / ±25% |
| Cowbell f_early | 635 Hz | ±20% |
| Rimshot tau / f_early / centroid | 0.0271 s / 452 Hz / 3092 Hz (n=11) | ±30% / ±20% / ±20% |
| Claves tau / f_early / centroid | 0.0208 s / 2489 Hz / 3592 Hz (n=11) | ±30% / ±20% / ±20% |
| Low Tom f_early | 86.1 Hz (pack median, n=12) | ±20% |
| Mid Tom f_early | 129.2 Hz (pack median, n=12) | ±20% |
| Hi Tom f_early | 172.3 Hz (pack median, n=12) | ±20% |
| Conga Lo f_early | 193.8 Hz (pack median, n=10) | ±20% |
| Conga Mid f_early | 258.4 Hz (pack median, n=10) | ±20% |
| Conga Hi f_early | 366.1 Hz (pack median, n=10) | ±20% |
| Maracas tau / centroid | 0.033 s / 7658 Hz | ±30% / ±20% |
| CH loudness | −26.8 LUFS (pack IQR −28.6..−26.0, n=12) | ±3 dB |
| OH loudness | −15.9 LUFS (pack IQR −20.0..−14.4, n=13) | ±4 dB |
| Cymbal loudness | −17.5 LUFS (pack IQR −22.7..−16.1, n=16) | ±4 dB |
| Accented solo peak, any voice | under −3 dBFS at velocity 127 | hard limit |

**The pooled tom and conga rows were replaced on 2026-09-02, because they
excluded the reference they came from.** They read "Toms f_early (LT/MT/HT
pooled) 129 Hz ±30%" and "Congas f_early 258 Hz ±30%" — one median standing for
three deliberately different pitches. A tuned voice's outer members cannot sit
inside ±30% of its middle one, and the arithmetic is not close:

| | pack median | pooled row's band | inside? |
|---|---|---|---|
| Hi Tom | 172.3 Hz | 90.3–167.7 Hz | **no** |
| Conga Hi | 366.1 Hz | 180.6–335.4 Hz | **no** |

So the MusicRadar pack — the source those medians were computed from — fails
its own criterion on two of six pitches. The rows could only ever have been
satisfied by an instrument whose toms and congas were *less* spread than the
machine's, which is the opposite of what they were meant to check. Both were
recorded as §5 failures during the METAL work and read as module defects; they
were the criteria.

Per-pitch targets are the pack's own medians, one per drum, taken from
`.reference-captures/tr808/musicradar_hits_stats.json` by the `E808_[HML][TC]-`
filename convention. ±20% is comfortable: the module's worst current deviation
is +9%.

**One finding the pooled row was hiding, recorded not fixed.** All three toms
run uniformly **+9%** sharp against the pack — 94/141/188 Hz against
86.1/129.2/172.3 — while the congas are within 3%. A consistent offset across
all three is a tuning constant, not three coincidences, and it is exactly the
kind of thing a single pooled median cannot show. Changing it alters the
module's sound, so it is not done here; it belongs to a listening pass.

**Measurement method:** `tools/measure_hits.py` on rendered one-shot WAVs
per voice — the same script that produced these targets, so Station C's
numbers are computed by the same code that proposed them.

The loudness rows were added on 2026-09-02, after §7 found a 15 dB level
error in the hats that every row above it passed cleanly. They are read
with the program's shared ruler (ITU-R BS.1770 K-weighting over a fixed
400 ms window from onset) on the same velocity-100 one-shots, and the
targets are this pack's own per-voice medians, resampled to 48 kHz in
memory and measured in place. A machine's own capture outranks the
cross-kit house norm where the two disagree, and here they do: the house
CY−BD median is +4.9 dB against this pack's −0.6 dB.

A loudness row cannot, on its own, tell a faithful rebuild from a gain
trim — makeup gain is spectrum-neutral and would satisfy it while holding
every tau and centroid row above. Read it alongside the spectral flatness
figures in §7, which a gain change does not move.

## 6. MCU budget and macro budget

Oscillator counts stay as-is per voice group (2-3 Notes per hit, as
today); the BD change trades one `LFO` shape for a two-stage bend, adding
no new oscillators. No `" - lean"` patch is expected — the added
Congas/Maracas branches reuse noise/tone building blocks already in the
file, not new DSP classes.

`MACRO_LABELS` is already at the 16/16 ceiling (tr808.py:9-13). **No new
macro is proposed.** The BD attack-jump/sigh folds onto the existing `BD
Decay` macro; Congas/Maracas get fixed relative tuning within the
existing model rather than a dedicated tune macro, consistent with the
hardware's shared-circuit voices (survey: "several sharing physical
circuits"). If a future acceptance pass finds this insufficient,
escalate a 17th-macro ask to Brad rather than silently exceed the
ceiling (roadmap §3, R6).

Table RAM, revised 2026-09-02: the METAL bank (§7) still occupies one
2048-sample `array("h")`, 4 KB, unchanged — the correction changed the
table's *contents*, not the number of tables. It now builds inside
`create()` rather than at import, so the cost is paid on the first
instrument at a given sample rate and `make_table`'s argument cache
serves every one after it. Measured cold build on the workspace
MicroPython (unix port, `ulab` present): 6.5 ms, against 0.2 ms cached.
The hats also gave up their per-sample `Biquad`, which the closed hat —
the most frequently struck voice in the kit — pays on every block.

## 7. Metal bank correction — hi-hats and cymbal (2026-09-02)

**What was wrong.** The closed hat measured −42.7 LUFS and the open hat
−34.2 on the program's shared ruler, against −23.8..−32.1 and
−18.7..−26.7 for the other nine kits' hats, and −26.8 / −15.9 for this
machine's own MusicRadar reference. Every criterion in §5 passed while
that was true, because §5 had no loudness row. Three causes, all in the
METAL bank, all measured:

1. **A truncated harmonic series.** `_METAL_PARTS` drove each square
   oscillator with `h in (1, 3, 5)`. A square wave's series does not stop
   at the fifth harmonic, and the truncation left 16 distinct partials in
   the whole table with 96.9% of the bank's power below the hi-hat's 8 kHz
   high-pass. Spectral flatness over 4–16 kHz — the band the hat lives in
   — measured 0.008 for the closed hat and 0.000 for the open hat and
   cymbal, against 0.302 / 0.116 / 0.114 for the reference pack. The model
   was making a metallic chord where the machine makes a dense hiss.

2. **The wrong octave.** `METAL_HZ` was 90.0, putting the bank at
   900–2700 Hz. The module's own comment cites the hardware bank as
   ~205/304/369/421/497/619 Hz, which against these multipliers is a
   least-squares fit of Σ(f·m)/Σ(m²) = 20.495 Hz — the code contradicted
   its own comment by two octaves. That is also what made the spectrum
   sparse: given the full square series below, 90 Hz puts 7 of the six
   oscillators' partials in the 6–11 kHz band; 20.5 Hz puts 35.

3. **Peak normalisation before the filter.** `make_table` normalises to
   peak, and the wavetable is the only gain stage these voices have —
   synthio clamps `Note.amplitude` at 1.0
   (`audioif/src/cpython/synthio.py:396-398`) and the kit hands the
   synthesizer straight to `Instrument` with no mixer or filter stage
   downstream (tr808.py:436). A table normalised
   *before* the band is applied spends its headroom on the oscillator
   fundamentals the hi-hat immediately discards; the 8 kHz high-pass then
   threw away 14.3 dB that nothing downstream could return. Raising
   `amp * 0.7` was never a fix: at 0.83 a fully accented hit already
   reaches the clamp, so the whole available makeup was 1.5 dB against a
   15 dB deficit.

**What changed**, all in `lib/audioinstruments/tr808.py`. No public
signature, macro number, `MACRO_LABELS` entry, `NOTE_MAP` assignment or
patch value moved, and the kit still resides at exactly 13 permanent
`synthio.Note` objects.

- METAL is built in `create()` by `_metal_table(sample_rate)` rather than
  at import, and its series is capped below both the table's harmonic
  ceiling and Nyquist. It is now alias-free at 22.05 kHz as well as 48;
  the shipped table's top partial aliased at any rate below 27 kHz.
- Each oscillator gets its full odd-harmonic square series — what the
  module's comment already claimed. 16 distinct partials become 114 at
  48 kHz.
- The hi-hat channel's band is applied *while the table is built*:
  Butterworth magnitudes, 4th-order high-pass at 5 kHz and 2nd-order
  low-pass at 13 kHz. Peak normalisation then lands on the signal the
  voices actually play, which is what the hardware's post-filter output
  stage does and the only place a fixed-point wavetable can carry it.
- Harmonics of different oscillators that land on the same table index
  are summed in **power**, not amplitude: six free-running oscillators do
  not phase-lock, so their coincidences do not add coherently. The
  coherent sum was manufacturing single lines several dB too strong — it
  is why `f_early` read exactly 8098 Hz for the closed hat, the open hat
  *and* the cymbal, three voices reporting one artifact.
- Partial signs are scattered from a seeded LCG (`noise_table`'s own
  generator, seed 687 — lowest crest of seeds 1..2500 at 48 kHz).
  `make_table` sums every partial in sine phase, which piles the bank's
  peaks onto one table index for 20.4 dB of crest; the reference pack's
  own hat hits measure 9–12 dB over their first 20 ms, so the aligned
  table was the outlier and the scatter is the correction. Phase is a
  free parameter here — the magnitude spectrum is untouched — and the
  scatter returns ~12 dB of headroom.
- Hats: no runtime filter, since the band is now in the table and this is
  the voice whose band sits highest; `amp * 0.7` → `amp * 0.8`.
- Cymbal: filter unchanged (`BAND_PASS` 8000 Hz Q=1.2); `amp * 0.85` →
  `amp * 0.8`. 0.8 is the largest multiplier that keeps a fully accented
  hit (amp 1.21 at velocity 127) under the amplitude clamp.

**Measured**, velocity-100 solo one-shots at 48 kHz. LUFS, peak, flatness
and crest on the shared ruler; centroid from `tools/measure_hits.py`;
reference figures are this pack's per-voice medians.

| | LUFS | peak dBFS | centroid Hz | flatness 4–16 kHz | crest, first 20 ms |
|---|---|---|---|---|---|
| CH before | −42.7 | −24.6 | 8827 | 0.008 | 12.5 dB |
| CH after | **−27.5** | −12.1 | 10081 | 0.103 | 9.9 dB |
| CH reference | −26.8 | −8.5 | 9687 | 0.302 | 12.2 dB |
| OH before | −34.2 | −24.6 | 9726 | 0.000 | 11.4 dB |
| OH after | **−19.0** | −12.1 | 10686 | 0.025 | 8.8 dB |
| OH reference | −15.9 | −7.6 | 9067 | 0.116 | 9.2 dB |
| CY before | −25.5 | −16.4 | 6177 | 0.000 | 14.3 dB |
| CY after | **−17.8** | −13.5 | 9112 | 0.016 | 9.9 dB |
| CY reference | −17.5 | −9.6 | 5238 | 0.114 | 10.6 dB |

Every §5 criterion passes: CH tau 0.0181 (0.0168–0.0312), CH centroid
10081 (7750–11624), OH tau 0.1303 (0.1211–0.2249), Cymbal tau 0.6723
(0.5425–1.0075), Cymbal f_early 6457 (5814–9690), and the three new
loudness rows. The other thirteen voices render byte-identical to before.
The full-kit probe peaks at −1.30 dBFS with no sample at full scale,
unchanged from before the correction; a fully accented solo hat peaks at
−6.5 dBFS and the cymbal at −7.9. CPython and the workspace MicroPython
produce identical digests for all sixteen voices.

**Known residual — the cymbal is brighter than the machine.** The bank is
voiced once, at the hi-hat's band, because the hat is the voice whose band
sits highest and can least afford a second pass; the cymbal takes its
share at runtime, which it can afford. What it cannot do is recover the
low-mid content the voicing removed: the reference cymbal carries 15.3% of
its power at 2–3 kHz and 11.6% at 3–4 kHz, ours carries 0.1% and 1.3%, and
its centroid reads 9112 against the pack's 5238 (and 6177 before this
change). Level, tau and f_early all land, and §5 states no cymbal centroid
criterion, so this is recorded rather than silently accepted. The fix is a
second voiced table for the cymbal's own band at +4 KB of table RAM —
worth taking if the cymbal reads thin on a listen against the pack.

**Also unresolved, found while measuring and outside this correction:**
Hi Tom `f_early` reads 187.5 Hz against the pooled 129 Hz ±30% row
(90.3–167.7) and Conga Hi reads 363.3 Hz against 258 Hz ±30%
(180.6–335.4). Both fail today and both failed before this change — the
pooled-median rows in §5 cannot hold three pitches of a tuned voice at
once. Either the rows want per-pitch targets or the tunings want a look.

**Awaiting Gate A:** `APPROVE ACCURACY DOSSIER tr808`
